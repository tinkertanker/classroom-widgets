# Classroom Widgets - Teacher App Development Environment
# Copy to packages/teacher/.env for local Vite development.

# Backend server URL used by the teacher app for API and Socket.IO connections.
VITE_SERVER_URL=http://localhost:3001

# Short.io defaults (optional). The Link Shortener and QR Code widgets work
# out of the box with a free service that needs no sign-up; these just
# pre-fill the runtime "Link Shortener…" setting (bottom bar menu) with your
# own Short.io account if you have one.
VITE_SHORTIO_API_KEY=
VITE_SHORTIO_DOMAIN=

# Umami Analytics (optional)
VITE_UMAMI_SCRIPT_URL=http://localhost:3003/script.js
VITE_UMAMI_WEBSITE_ID=

# Enable debug mode (optional)
# VITE_DEBUG=true
